# ============================================================================
# DaoGAN - Requirements for Reproducible Experiments
# ============================================================================
# Python: >=3.11,<3.12 (tested: 3.11.4)
# CUDA: 12.1 (NVIDIA driver >= 530.30.02)
# OS: Windows 10/11, Linux, macOS
#
# 安装命令 (Windows + CUDA 12.1):
#   pip install -r requirements.txt \
#     --index-url https://download.pytorch.org/whl/cu121 \
#     --extra-index-url https://pypi.org/simple
#   pip install -e torch-fidelity --no-deps
# ============================================================================

# --------------------------------------------------------------------------
# Core AI Framework (CUDA 12.1)
# --------------------------------------------------------------------------
# 必须用官方索引源安装，镜像可能无 +cu121 版本
torch==2.3.1+cu121
torchvision==0.18.1+cu121

# --------------------------------------------------------------------------
# 数值计算与科学统计 (训练 + 论文表格生成)
# --------------------------------------------------------------------------
numpy==1.26.0
scipy==1.15.3
pandas==2.2.3


# --------------------------------------------------------------------------
# 绘图库 (论文图像生成)
# --------------------------------------------------------------------------
matplotlib==3.10.3


# --------------------------------------------------------------------------
# 统计检验与论文输出 
# --------------------------------------------------------------------------
statsmodels==0.14.6      # Welch t-test, Hedges'g, power analysis, FDR correction
openpyxl==3.1.5          # Excel 输出带格式 (Font, PatternFill, Border 等)

# --------------------------------------------------------------------------
# 图像处理与可视化
# --------------------------------------------------------------------------
pillow==10.4.0           # PIL.Image 保存可视化网格

# --------------------------------------------------------------------------
# 进度条与日志
# --------------------------------------------------------------------------
tqdm==4.67.1             # 训练进度条
tensorboard==2.19.0      # 日志记录与可视化 (CLI: tensorboard --logdir logs)

# --------------------------------------------------------------------------
# 本地修改包 (不在此安装，见下方说明)
# --------------------------------------------------------------------------
# torch-fidelity (modified): 本地子模块，需单独安装
# 命令: pip install -e torch-fidelity --no-deps
# 原因: 避免依赖解析覆盖 torch/torchvision 版本